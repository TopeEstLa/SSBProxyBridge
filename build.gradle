group 'SSBProxyBridge'
version = "0.0.1"

allprojects {
    apply plugin: 'java'

    repositories {
        mavenCentral()
    }

}

project.ext {
    targetDir = file("./target/")
    buildNumber = System.getenv("BUILD_NUMBER")
}